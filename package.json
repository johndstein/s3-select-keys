{
  "name": "s3-select-keys",
  "keywords": ["S3", "AWS", "search", "query", "SQL"],
  "version": "0.0.3",
  "description": "Search a list of keys (files) with S3 Select SQL queries.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/johndstein/s3-select-keys.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Stein",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.449.0"
  },
  "devDependencies": {
    "merge2": "^1.2.3"
  }
}
